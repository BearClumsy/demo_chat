# 2026-07-22

## Code style & tooling
- Applied the CLAUDE.md `var`-over-explicit-type guideline across the [[rag-pipeline|rag-pipeline.md]]
  package and its tests — builder chains, static factories, and plain calls whose type is already
  clear from the RHS now use `var` instead of repeating the type name. Deliberately left fields,
  parameters, and return types untouched (the guideline is local-variable-only), so this is a
  readability pass, not a broad type-inference sweep.
- Expanded the "Java Style" section of `CLAUDE.md` itself to document the rule with a concrete
  example, rather than leaving it an unwritten convention only visible in the diff.
- Ran `spotlessApply` to reformat the touched files and drop an import that became unused once its
  type name was replaced by `var` — the formatter, not a manual edit, is the source of truth for
  the final layout.
